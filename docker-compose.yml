services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
      - "9999:9999"
    volumes:
      - .:/usr/src/app
    environment:
      - NODE_ENV=development
    command: ["npm", "run", "dev"]
    tty: true
    stdin_open: true
